{
  "name": "@story-mapper/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b .",
    "test": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@story-mapper/shared": "0.0.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
